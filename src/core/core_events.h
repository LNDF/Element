#pragma once

namespace element {
    namespace events {
        
        struct close {

        };

        struct update {
            double delta_time;
        };

    } // namespace events
} // namespace element
